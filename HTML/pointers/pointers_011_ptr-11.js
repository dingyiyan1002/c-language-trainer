/**
 * 指针面试题 09 - 指针数组与数组指针
 * 难度：⭐⭐⭐⭐⭐
 * 考点：指针数组、数组指针、复杂声明
 */

export const pointer_interview_009 = {
  title: '指针数组 vs 数组指针',
  difficulty: '高级',
  question: `解释以下声明的含义：

1. int *arr1[5]
2. int (*arr2)[5]
3. int *arr3[3][4]
4. int (*arr4[3])[4]

并说明它们的区别和使用场景。`,
  answer: `详细解析：

1. int *arr1[5] （指针数组）
   - 含义：包含 5 个 int* 指针的数组
   - 每个元素都是 int* 类型
   - 使用：arr1[i] 是指针，*arr1[i] 是 int 值
   - 场景：存储多个字符串、动态二维数组

2. int (*arr2)[5] （数组指针）
   - 含义：指向包含 5 个 int 的数组的指针
   - arr2 是指针，*arr2 是包含 5 个 int 的数组
   - 使用：(*arr2)[i] 访问元素
   - 场景：指向二维数组的一行

3. int *arr3[3][4] （二维指针数组）
   - 含义：3x4 的数组，每个元素是 int*
   - 共 12 个指针

4. int (*arr4[3])[4] （数组的指针的数组）
   - 含义：包含 3 个元素的数组
   - 每个元素是指向 [4] 个 int 的数组的指针

记忆技巧（从内到外读）：
- arr1[5] 先结合 → arr1 是数组 → int* 类型 → 指针数组
- (*arr2) 先结合 → arr2 是指针 → 指向 [5] 数组 → 数组指针

示例：
int row[5] = {1,2,3,4,5};
int (*p)[5] = &row;  // p 指向整个数组
printf("%d", (*p)[2]);  // 输出 3

char *strs[3] = {"A", "B", "C"};  // 指针数组存字符串`,
  keyPoints: [
    'int* arr[N] 是指针数组',
    'int (*arr)[N] 是数组指针',
    '[] 优先级高于*',
    '用括号改变结合顺序'
  ],
  commonMistakes: [
    '混淆指针数组和数组指针',
    '不理解 [] 和*的结合优先级',
    '不会读复杂声明',
    '不知道如何使用数组指针'
  ]
};
