/**
 * 文件操作面试题 13 - 文件打开模式详解
 * 难度：⭐⭐⭐
 * 考点：fopen 各种模式的区别
 */

export const file_interview_13 = {
  title: '文件打开模式详解',
  difficulty: '中级',
  question: `以下 fopen 模式有什么区别？

1. "r"  vs  "r+"
2. "w"  vs  "w+"
3. "a"  vs  "a+"
4. "wx" 是什么模式？`,
  answer: `文件打开模式对比：

1. "r" vs "r+":
   - "r": 只读，文件必须存在
   - "r+": 读写，文件必须存在

2. "w" vs "w+":
   - "w": 只写，文件存在则清空，不存在则创建
   - "w+": 读写，文件存在则清空，不存在则创建

3. "a" vs "a+":
   - "a": 只写，追加模式，写入总是在末尾
   - "a+": 读写，读取任意位置，写入总是在末尾

4. "wx":
   - 独占创建模式，文件存在则失败
   - C11 标准引入，避免竞态条件

核心要点：
- r/w/a 分别代表读/写/追加
- + 表示同时支持读写
- x 表示独占创建（exist 检查）`,
  keyPoints: [
    'r 模式文件必须存在',
    'w 模式会清空文件',
    'a 模式写入总在末尾',
    '+ 表示读写双模式',
    'x 表示独占创建'
  ],
  commonMistakes: [
    '用"w"打开想追加的文件导致内容丢失',
    '用"r"打开不存在的文件导致 NULL',
    '不理解"a"模式写入位置固定在末尾',
    '忽略 fopen 返回值检查'
  ]
};
